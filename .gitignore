# Local Claude Code settings
.claude/settings.local.json

# Version backups
Backup of versions/

# STEPBible raw data files (large, re-downloadable)
stepbible/*.txt
stepbible/*.csv

# Vite build output
dist/
node_modules/
package-lock.json

# Secrets
.env
.env.local
.env.*.local

# Temporary patch scripts
patch_app.js
patch_app.cjs
patch_audio.cjs

# Audio timestamp JSONs — large, only needed for Android/iOS builds, generated locally
public/timestamps/

# Android/iOS Capacitor builds — generated, not source
android/
# ios/
